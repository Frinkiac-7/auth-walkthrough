{{log model.text}}
<form {{action 'deleteEntry' model on='submit'}}>
  <div class="form-group">
    <label for="exampleText">Delete Record Form</label>
    {{input type="text" class="form-control" value=model.text}}
  </div>
  <button type="submit" class="btn btn-default">Delete this record?</button>
</form>
{{!-- {{outlet}} --}}
